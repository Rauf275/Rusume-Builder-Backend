# Local development only. On Railway (or similar), DATABASE_URL is injected
# automatically once a PostgreSQL database is attached to this service —
# you don't need to set it by hand there.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/resume_builder
PORT=3001
